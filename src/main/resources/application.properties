# server.port = 8080
# server.servlet.session.persistent=false

# gradebook service
gradebook.url = http://localhost:8081
gradebook.service = MQ
# registration service
registration.url = http://localhost:8080
registration.service = default

# database properties
# NOTE: Commented this out because it said it was deprecated, may need it for initializing the DB though.
# spring.datasource.initialization-mode=always
spring.datasource.url = jdbc:mysql://localhost:3306/register
spring.datasource.username = root
spring.datasource.password = NotSoSuper!932
spring.jpa.generate-ddl=true
# spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true

server.servlet.session.persistent=false
spring.jpa.hibernate.ddl-auto = update
spring.datasource.hikari.maximum-pool-size=5

# debug = true
# logging.level.org.springframework.security = TRACE
